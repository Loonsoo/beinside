{
  "name": "beinside",
  "version": "1.0.0",
  "private": true,
  "description": "BeInside — 혼자라고 느낄 때, 가장 먼저 닿는 곳",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test tests/integrity.test.js"
  },
  "devDependencies": {
    "vite": "^8.0.2"
  }
}
